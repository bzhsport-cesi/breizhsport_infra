services:
  app:
    image: {{ image_name }}
    restart: always
    expose:
      - "3000"
    networks:
      - frontend_net

  nginx:
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - frontend_net

networks:
  frontend_net: